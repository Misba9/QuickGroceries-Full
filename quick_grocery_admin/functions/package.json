{
  "name": "quick-grocery-admin-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext .ts src",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "seed:admin": "node scripts/seed_admin.cjs"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "twilio": "^5.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
