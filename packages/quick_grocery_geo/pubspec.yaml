name: quick_grocery_geo
description: Shared geolocation, navigation, and routing utilities for Quick Groceries apps.
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  http: ^1.3.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_lints: ^5.0.0
